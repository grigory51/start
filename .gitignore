# macOS
.DS_Store

# install backups
*.bak

# uv / python
.venv/
__pycache__/

# per-machine overrides (машино-специфичные вкл/выкл поверх config.toml)
config.local.toml

# plugin seed (производный — пересобирается на каждом `make up`)
.seed/

# OMC runtime (legacy, если вдруг создаётся)
.omc/
