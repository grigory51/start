# macOS
.DS_Store

# install backups
*.bak

# uv / python
.venv/
__pycache__/

# per-machine overrides (машино-специфичные вкл/выкл поверх config.toml)
config.local.toml

# plugin seed (производный — пересобирается на каждом `make up`)
# .seed — стабильный symlink на активный буфер; .seed.store/ — сами буферы (double-buffer)
.seed
.seed.store/

# OMC runtime (legacy, если вдруг создаётся)
.omc/
