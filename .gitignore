# macOS
.DS_Store

# install backups
*.bak

# uv / python
.venv/
__pycache__/

# plugin seed (производный — пересобирается на каждом `make up`)
.seed/

# OMC runtime (legacy, если вдруг создаётся)
.omc/
