# Ansible for local provisioning of machines from the "Commands" domain.
# Wrappers (e.g. scripts/provision.sh) run playbooks against localhost (connection=local).
# Roles live in ./roles.
[defaults]
roles_path = ./roles
interpreter_python = auto_silent
retry_files_enabled = false
localhost_warning = false
gathering = smart
