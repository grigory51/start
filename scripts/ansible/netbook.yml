---
# Playbook to provision a headless Debian netbook. Applied LOCALLY
# (scripts/provision.sh netbook -> ansible-playbook -i 'localhost,' -c local netbook.yml).
- name: Provision netbook (Debian, headless)
  hosts: all
  gather_facts: true          # ansible_env.HOME needed for dotfiles
  roles:
    - netbook
